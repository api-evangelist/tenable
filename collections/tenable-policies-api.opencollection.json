{
  "opencollection": "1.0.0",
  "info": {
    "name": "Downloads About Policies API",
    "version": "1.0.0"
  },
  "request": {
    "auth": {
      "type": "apikey",
      "key": "Authorization",
      "value": "{{Authorization}}",
      "placement": "header"
    }
  },
  "items": [
    {
      "info": {
        "name": "Policies",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "List policies",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://www.tenable.com/downloads/api/v2/policies"
          },
          "docs": "Returns a list of policies (scan templates).\n\n**Note:** Policies are referred to as scan templates in the user interface. The term policy is only used by the API. The two terms refer to the same feature. <div class=\"perms-callout\">Requires the Standard [32] user role or the `VM.VM_SCAN.VM_SCAN_USER_TEMPLATE.READ` custom role privilege. See [Roles](doc:roles).</div>"
        },
        {
          "info": {
            "name": "Create policy",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://www.tenable.com/downloads/api/v2/policies",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Creates a policy (scan template).\n\n**Note:** Policies are referred to as scan templates in the user interface. The term policy is only used by the API. The two terms refer to the same feature. <div class=\"perms-callout\">Requires the Standard [32] user role or the `VM.VM_SCAN.VM_SCAN_USER_TEMPLATE.CREATE` custom role privilege. See [Roles](doc:roles).</div>"
        },
        {
          "info": {
            "name": "Copy policy",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://www.tenable.com/downloads/api/v2/policies/:policy_id/copy",
            "params": [
              {
                "name": "policy_id",
                "value": "",
                "type": "path",
                "description": "The id of the policy to copy."
              }
            ]
          },
          "docs": "Copies a policy (scan template).\n\n**Note:** Policies are referred to as scan templates in the user interface. The term policy is only used by the API. The two terms refer to the same feature. <div class=\"perms-callout\">Requires the Standard [32] user role and the Can Execute [32] scan template (policy) permission. Alternatively, requires the `VM.VM_SCAN.VM_SCAN_USER_TEMPLATE.CREATE` custom role privilege. See [Roles](doc:roles) and [Permissions](doc:permissions).</div>"
        },
        {
          "info": {
            "name": "Import policy",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://www.tenable.com/downloads/api/v2/policies/import",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Imports an existing policy (scan template) that was uploaded using the [POST /file/upload](ref:file-upload) endpoint. Only policies (scan templates) in .nessus format can be imported.\n\n**Note:** Policies are referred to as scan templates in the user interface. The term policy is only used by the API. The two terms refer to the same feature. <div class=\"perms-callout\">Requires the Standard [32] user role or the `VM.VM_SCAN.VM_SCAN_USER_TEMPLATE.CREATE` custom role privilege. See [Roles](doc:roles"
        },
        {
          "info": {
            "name": "Export policy",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://www.tenable.com/downloads/api/v2/policies/:policy_id/export",
            "params": [
              {
                "name": "policy_id",
                "value": "",
                "type": "path",
                "description": "The ID of the policy to export."
              }
            ]
          },
          "docs": "Exports the specified policy (scan template) in nessus format.\n\n**Note:** Policies are referred to as scan templates in the user interface. The term policy is only used by the API. The two terms refer to the same feature.<div class=\"perms-callout\">Requires the Standard [32] user role and the Can Execute [32] scan template (policy) permission. See [Roles](doc:roles) and [Permissions](doc:permissions).</div>"
        },
        {
          "info": {
            "name": "List policy details",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://www.tenable.com/downloads/api/v2/policies/:policy_id",
            "params": [
              {
                "name": "policy_id",
                "value": "",
                "type": "path",
                "description": "The ID of the policy to retrieve."
              }
            ]
          },
          "docs": "Returns the details for the specified policy (scan template).\n\n**Note:** Policies are referred to as scan templates in the user interface. The term policy is only used by the API. The two terms refer to the same feature. <div class=\"perms-callout\">Requires the Standard [32] user role and the Can View [16] scan template (policy) permission. Alternatively, requires the `VM.VM_SCAN.VM_SCAN_USER_TEMPLATE.READ` custom role privilege. See [Roles](doc:roles) and [Permissions](doc:permissions).</div>"
        },
        {
          "info": {
            "name": "Update policy",
            "type": "http"
          },
          "http": {
            "method": "PUT",
            "url": "https://www.tenable.com/downloads/api/v2/policies/:policy_id",
            "params": [
              {
                "name": "policy_id",
                "value": "",
                "type": "path",
                "description": "The ID of the policy to change."
              }
            ]
          },
          "docs": "Updates an existing policy (scan template). \n\n**Note:** Policies are referred to as scan templates in the user interface. The term policy is only used by the API. The two terms refer to the same feature.<div class=\"perms-callout\">Requires the Standard [32] user role and the Can Execute [32] scan template (policy) permission. Alternatively, requires the `VM.VM_SCAN.VM_SCAN_USER_TEMPLATE.EDIT` custom role privilege. See [Roles](doc:roles) and [Permissions](doc:permissions).</div>"
        },
        {
          "info": {
            "name": "Delete policy",
            "type": "http"
          },
          "http": {
            "method": "DELETE",
            "url": "https://www.tenable.com/downloads/api/v2/policies/:policy_id",
            "params": [
              {
                "name": "policy_id",
                "value": "",
                "type": "path",
                "description": "The ID of the policy to delete."
              }
            ]
          },
          "docs": "Deletes a policy (scan template). \n\n**Note:** Policies are referred to as scan templates in the user interface. The term policy is only used by the API. The two terms refer to the same feature.<div class=\"perms-callout\">Requires the Standard [32] user role and the Can Execute [32] scan template (policy) permission. Alternatively, requires the `VM.VM_SCAN.VM_SCAN_USER_TEMPLATE.DELETE` custom role privilege. See [Roles](doc:roles) and [Permissions](doc:permissions).</div>"
        }
      ]
    }
  ],
  "bundled": true
}